# PROTOCOL.md — demo-project

Conforms to: ai-handoff-protocol SPEC.md v0.1 (see repo root `SPEC.md` for
full rules). This file is the trimmed, project-local copy referenced by
`SPEC.md` §3.

## Read this before doing anything

1. This file.
2. `memory/INDEX.md`.
3. `status/CURRENT_STATUS.md`.
4. Whatever directory matches your current task.

If you suspect you're working from a compacted/summarized context, re-read
1–3 before continuing.

## Rules specific to this project

(None yet — this is a fictional demo with no accumulated project-specific
exceptions. A real project would list things like "don't touch `vendor/`"
or "always run `northwind-api` migration checks before updating
`CURRENT_STATUS.md`" here.)
