[project]
name = "ai-baton-tool"
version = "0.0.17"
description = "Portable, auditable, file-first handoff protocol for AI assistants."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
  "jsonschema>=4.0",
  "pyyaml>=6.0",
]

[project.scripts]
ai-baton = "ai_baton.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ai_baton"]

[dependency-groups]
dev = [
  "pytest>=8.0",
]
